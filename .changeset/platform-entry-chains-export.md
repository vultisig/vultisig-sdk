---
'@vultisig/sdk': patch
---

Export the shared `chains` tx-builder namespace from the Node, browser, electron, and chrome-extension SDK entrypoints so the documented platform-specific signing surface is reachable outside React Native.
