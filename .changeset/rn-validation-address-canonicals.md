---
'@vultisig/sdk': patch
---

Re-export the root validation/address-format helper family from `@vultisig/sdk/react-native` so React Native consumers can use the canonical SDK surfaces for amount/fee normalization and address/chain-prefix validation without deep imports or local copies.
