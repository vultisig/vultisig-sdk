---
'@vultisig/sdk': patch
---

Publish `./tools/policy` and `./tools/gas` as real SDK subpaths (mirroring the existing `./tools/parse`, `./tools/defi`, `./tools/bridge`, `./tools/balance` pattern). Both were already coherent, pure SDK surfaces (`policy.evaluate` / `policy.checkInvariants`, `compareCosts` / `getChainGasPriceGwei` / UTXO fee-rate helpers) exported from the SDK root but not addressable as narrow package subpaths, pushing consumers toward broader root imports. Wired dedicated JS + `.d.ts` bundle generation in the Rollup configs, added the export-map entries, and extended the public-api regression test and tarball smoke script to cover both.
