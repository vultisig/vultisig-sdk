---
'@vultisig/sdk': patch
---

Expose the balance, prep, and swap helper groups on the root and React Native SDK surfaces while preserving existing flat exports and React Native's deferred preparation and balance wrappers.
