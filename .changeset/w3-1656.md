---
'@vultisig/sdk': patch
---

Cover the `@vultisig/sdk/seedphrase` subpath in the package-export-map regression test. The subpath itself (`package.json` exports entry + both the JS and `.d.ts` rollup bundle configs) was already wired, but untested — the canonical mnemonic normalization/validation/import helper family (`normalizeMnemonic`, `detectMnemonicLanguage`, `findInvalidWords`, `cleanMnemonic`, `SeedphraseValidator`, and the import/discovery services) is reachable via `import from '@vultisig/sdk/seedphrase'`, this just closes the coverage gap so a future accidental removal of the wiring fails CI instead of silently regressing.
