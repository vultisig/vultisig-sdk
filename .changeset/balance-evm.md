---
'@vultisig/sdk': minor
---

Add `sdk.balance.evm` (`getEvmBalances`): a multi-token EVM balance read (native coin + any number of ERC-20s) via RPC, with per-token on-chain `decimals`/`symbol` resolution and raw base-unit + human-readable output.
