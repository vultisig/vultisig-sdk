---
'@vultisig/core-chain': minor
'@vultisig/sdk': minor
---

Add an opt-in `raced-public-rpc` EVM broadcast strategy so callers can bypass Blink-Protect silent drops. Default single-endpoint broadcast is unchanged.
