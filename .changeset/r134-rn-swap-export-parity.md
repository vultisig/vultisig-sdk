---
"@vultisig/sdk": patch
"@vultisig/cli": patch
---

Export root swap helpers from the React Native SDK entrypoint so mobile consumers can use the same Across, Jupiter, and Skip canonicals without local copies.
