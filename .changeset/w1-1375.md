---
'@vultisig/sdk': patch
---

Removed two dead exports flagged by the whole-stack audit: `RIVER_FACTORY_ABI` (`packages/sdk/src/tools/defi/river/abi.ts`) had zero importers anywhere in the workspace, and `BITTENSOR_BASE_FEE_RAO` (`packages/sdk/src/tools/balance/bittensor.ts`) was defined once and referenced nowhere — an unfinished fee computation (`BITTENSOR_BASE_FEE_HUMAN`, the display-string sibling, is still used and untouched). Neither constant was part of the public SDK surface. The mpc-types/mpc-wasm circular-dependency architecture problem this issue also raises is out of scope here — extracting a shared leaf package for the MPC engine/types is a genuine multi-package restructure, not a small fix.
