---
'@vultisig/mcp': patch
---

Float the workspace hono resolution pin from exact 4.12.25 to ^4.12.34 so the seven published advisories on the frozen version are patched. hono is the HTTP layer under @vultisig/mcp via @modelcontextprotocol/sdk.
