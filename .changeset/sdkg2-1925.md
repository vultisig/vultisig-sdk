---
'@vultisig/mcp': patch
---

Pin the transitive Hono HTTP layer used by `@vultisig/mcp` through `@modelcontextprotocol/sdk` to the patched, cooldown-eligible 4.13.7 release instead of vulnerable 4.12.25.
