---
'@vultisig/sdk': minor
---

Expose native coin display symbols, chain-specific ticker aliases, and unambiguous native-chain lookup from the SDK root, token namespace, and React Native entry points.
