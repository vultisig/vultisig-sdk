---
'@vultisig/sdk': patch
'@vultisig/cli': patch
---

Export the generic swap canonicals from the React Native SDK entrypoint so RN consumers can use the same swap helpers as the root SDK surface without deep imports.
