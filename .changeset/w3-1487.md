---
'@vultisig/sdk': patch
---

Publish `@vultisig/sdk/tools/swap` as a real package subpath, following the same pattern already shipped for `./tools/parse`, `./tools/defi`, `./tools/bridge`, and `./tools/balance`. The swap-tool barrel (`findSwapQuote`, Jupiter direct-swap builder, Across/Astroport/Skip quote+build helpers, and swap safety guards like `PriceImpactTooHighError`/`MAX_PRICE_IMPACT_PCT`) already existed as a coherent narrow module but was only reachable via the broad root entrypoint or a private deep import.
