---
'@vultisig/sdk': patch
---

Centralize the Cosmos validator-role (`valoper`/`valcons`) HRP classifier. `tools/prep/cosmosStaking.ts` and `tools/prep/ibcTransfer.ts` each carried their own copy of the same fund-safety rule (a validator operator/consensus address is not a spendable account) alongside the canonical, already-exported `validatorRoleForHrp` in `tools/swap/skip/cosmosAddressGuard.ts`. Both now import the shared classifier instead of a local duplicate, so the rule can't drift between the staking, IBC, and Skip-swap call sites. No behavior change — same classification logic, same error messages.
