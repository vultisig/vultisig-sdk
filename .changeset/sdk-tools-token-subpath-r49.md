---
'@vultisig/sdk': patch
'@vultisig/cli': patch
---

Publish `@vultisig/sdk/tools/token` as a first-class package subpath so consumers can import canonical token helpers without routing through the root SDK entrypoint.
