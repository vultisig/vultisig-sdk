---
'@vultisig/sdk': patch
'@vultisig/cli': patch
---

Export the canonical Blockaid EVM chain map and supported-chain list from the root and React Native SDK entrypoints so first-party consumers can stop maintaining local mirrors.
