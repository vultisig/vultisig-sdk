---
'@vultisig/sdk': minor
---

Expose the documented `sdk.amount` namespace (`sdk.amount.convert`, `toBaseUnits`, `toHumanUnits`, `fiatToCrypto`, `cryptoToFiat`) as a root SDK export and as a `Vultisig` instance handle, while preserving the existing flat exports.
