---
'@vultisig/core-chain': patch
'@vultisig/core-mpc': patch
'@vultisig/sdk': patch
---

Fail closed when LI.FI returns a destination outside its official chain-scoped Diamond deployments or when a SwapKit EVM transaction destination differs from the screened target address returned by SwapKit.
