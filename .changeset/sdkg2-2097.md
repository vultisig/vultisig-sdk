---
'@vultisig/sdk': patch
---

Export canonical token-candidate helpers (`normalizeTokenCandidates`, `pickClearTokenCandidate`, `findContractIdentity`, `classifyTokenInput`) from the root and React Native SDK surfaces so consumers stop duplicating `searchToken` interpretation.
