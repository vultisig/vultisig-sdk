---
'@vultisig/sdk': patch
'@vultisig/cli': patch
---

Add verified EIP-712 signing through vault.signTypedData with canonical low-S signatures, explicit credentials and cancellation. Delegate CLI typed-data signing to the SDK while preserving batch results.
