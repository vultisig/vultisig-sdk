---
'@vultisig/sdk': patch
'@vultisig/cli': patch
---

Export `buildBalancerV3SwapCalldata` from the root SDK surface so Node and browser consumers can import it the same way React Native consumers already can.
