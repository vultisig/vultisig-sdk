---
'@vultisig/sdk': patch
---

Added a public-export regression test pinning the `sdk.evm`, `sdk.token`, and `sdk.cosmos.gov` namespace handles that `packages/sdk/CHANGELOG.md` documents. These namespaces were already shipped (PR #1890) alongside the existing flat exports, but had no test locking the shape in place; this closes that gap so a future refactor can't silently drop the namespace re-export.
