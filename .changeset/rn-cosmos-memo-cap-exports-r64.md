---
"@vultisig/sdk": patch
"@vultisig/cli": patch
---

Export the canonical Cosmos memo-cap helpers from `@vultisig/sdk/react-native` so React Native consumers can import the same SDK-owned memo-length rules as the root package.
