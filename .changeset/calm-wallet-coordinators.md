---
'@vultisig/sdk': minor
---

Support external wallet coordinators with serialized ECDSA extended public keys, explicit `signBytes` derivation paths, and optional non-blocking Fast Vault keygen completion signaling. Also make React Native vault-backup format detection work with plain `Uint8Array` views.
