---
'@vultisig/sdk': patch
---

Export `isValidTokenId` from both the root SDK entry and `@vultisig/sdk/react-native` (RN gets a `WalletCoreLike`-accepting wrapper, matching the existing `isValidAddress`/`deriveAddress`/`getCoinType` pattern). The canonical helper already existed in `@vultisig/core-chain` and validates non-address token identifiers — Sui Move struct tags and XRPL composite `currency.issuer` ids — falling back to plain address validation for every other chain, but neither public SDK surface exposed it, pushing Sui/XRPL token-id validation back into app/backend-local shims.
