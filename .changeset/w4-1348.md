---
'@vultisig/sdk': patch
---

Export `fromChainAmount` and `fromChainAmountDisplay` from the SDK root and `@vultisig/sdk/react-native` entry points, alongside the already-exported `toChainAmount`/`fromChainAmountExact`. All four canonical amount-conversion helpers now round-trip base-units to human amounts through one shared source instead of downstream consumers hand-rolling `10 ** decimals` division/multiplication.
