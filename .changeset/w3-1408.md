---
'@vultisig/sdk': patch
---

Re-export the pure validate-normalizer, address-format/chain-prefix, and `policy` helper families from `@vultisig/sdk/react-native`, in parity with the root SDK entrypoint. `amountMatches`/`computeEvmFee`/`decimalsFor`/`feeMatches`/`isValidTokenSymbolFormat`/`normalizeTokenSymbol`/`scaleHumanToRaw`/`scaleRawToHuman`/`tokenDecimals`, `canonicalChainTag`/`classifyAddress`/`isAddressValidForChain`/`isSolanaAddress`/`supportedChainTags`/`address`/`validate`/`checkChainPrefix`, and the `policy` namespace (`policy.evaluate`/`policy.checkInvariants`) already existed as pure, vault-free, RN-safe helpers exported from the root entry, but were missing from the RN entry — pushing mobile consumers toward local copies or deep imports for amount/fee normalization, address validation, and intent/envelope policy diffing.
