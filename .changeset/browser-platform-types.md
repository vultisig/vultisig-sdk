---
'@vultisig/sdk': patch
'@vultisig/cli': patch
---

Emit dedicated browser and chrome-extension declaration bundles so the public subpath exports expose the platform-specific types they already ship at runtime.
