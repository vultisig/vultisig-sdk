---
'@vultisig/sdk': patch
'@vultisig/cli': patch
---

Export the canonical `buildCosmosWasmExecuteTx` helper and its types from `@vultisig/sdk/react-native` so mobile consumers can import the same supported surface as root SDK callers.
