---
"@vultisig/sdk": patch
"@vultisig/cli": patch
---

Publish the `@vultisig/sdk/tools/dex` subpath so consumers can import the dedicated DEX quote helpers without routing through the broad root entry.
