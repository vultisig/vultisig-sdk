---
'@vultisig/sdk': patch
---

Add real published subpath bundles for `@vultisig/sdk/tools/parse` and `@vultisig/sdk/tools/defi`, including dedicated declaration output and package-name import smoke coverage.
