---
'@vultisig/sdk': patch
---

Accept Vultisig canonical source-chain names in `supportedIbcDestinationsFrom()` so the exported route-discovery helper matches `prepareIbcTransfer()`'s already-supported alias contract.
