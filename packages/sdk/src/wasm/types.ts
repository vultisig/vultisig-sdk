/**
 * WASM types
 *
 * Note: DKLS and Schnorr WASM modules are handled internally by core's
 * initializeMpcLib() - they don't need SDK-level configuration.
 *
 * WalletCore WASM is managed by SharedWasmRuntime.
 */

// This file is kept for backwards compatibility but no longer exports anything
// WASM management is now handled automatically
